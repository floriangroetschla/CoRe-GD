from setuptools import setup

setup(
   name='neuraldrawer',
   version='1.0',
   description='The NeuralDrawer package',
   author='tbd',
   author_email='anonymous',
   packages=['neuraldrawer'],  #same as name
   install_requires=[], #external packages as dependencies
)
